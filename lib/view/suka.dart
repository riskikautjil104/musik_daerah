import 'package:flutter/material.dart';

class Suka extends StatelessWidget {
  const Suka({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Suka Page'),
    );
  }
}
